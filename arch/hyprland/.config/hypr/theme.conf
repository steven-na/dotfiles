# Theme / color configuration — generated by wallust
# Run set-wallpaper <path> to regenerate
#
# If ~/.cache/wallust/hyprland-colors.conf doesn't exist yet (first boot),
# the fallback colors below are used until you set a wallpaper.

source = ~/.cache/wallust/hyprland-colors.conf
