#!/usr/bin/env bash
# hyprland-workspaces streams JSON arrays on every workspace change.
# Filter out special/scratchpad workspaces (negative IDs).
hyprland-workspaces eDP-1 | while IFS= read -r line; do
    echo "$line" | jq -c '[.[] | select(.id > 0)]'
done
